export { COOKIE_NAME, ONE_YEAR_MS } from "@shared/const";

// Keep authentication inside the app. The portfolio section contains the login/signup form.
export const getLoginUrl = () => {
  return "/#portfolio";
};
